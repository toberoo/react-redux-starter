const initialState = {
    title: 'I am reacting and reduxing like a pro!'
}

function root(state = initialState, action) {
    switch(action.type) {
        default: return state
    }
}

export default root